#function to find gcd of 2 positive numbers (euclidean algorithm)

def gcd(a,b):
    if b==0:
        return a
    else:
        return gcd(b,a % b)
a=int(input("number a?  "))
b=int(input("number b?  "))
print(gcd(a,b))